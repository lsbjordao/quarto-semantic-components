---
title: "Quarto Semantic Components — exemplos"
format:
  html:
    toc: true
execute:
  echo: true
filters:
  - semantic-components
---

Esta página demonstra os componentes da extensão **Quarto Semantic Components**. Em cada seção, o código-fonte aparece **antes** do resultado renderizado.

## Defaults de projeto no `_quarto.yml`

Este próprio repositório usa defaults compartilhados:

```yaml
extensions:
  badge:
    - key: stable
      label: Estável
      colour: springgreen
      fg: "#102a18"
      appearance: solid
    - key: experimental
      label: Experimental
      class: bg-info
      appearance: solid

  steps:
    dot-color: "#8c959f"
    dot-size: "0.72rem"
    line-color: "#d0d7de"
    line-width: "1.5px"

  file-tree:
    icons: devicon

  git-tree:
    line-color: "#9aa0a6"
    line-width: "2px"
    node-size: "0.68rem"
    lane-gap: "0.82rem"
```

A precedência é **defaults do projeto → preset → componente → item individual**.

## Badges inline

> O shortcode público agora é `badge`. Como [`mcanouil/quarto-badge`](https://github.com/mcanouil/quarto-badge) usa o mesmo nome, o projeto deve escolher qual extensão de badge deseja usar.

### Presets definidos no projeto

```markdown
{{< badge stable >}}
{{< badge experimental >}}
{{< badge "Release estável" key="stable" >}}
```

**Output:** {{< badge stable >}} {{< badge experimental >}} {{< badge "Release estável" key="stable" >}}

### Personalização livre

```markdown
{{< badge "Beta" >}}
{{< badge "Estável" type="success" icon="✓" >}}
{{< badge "Experimental" type="warning" >}}
{{< badge "v0.6.0" type="accent" appearance="solid" >}}

{{< badge "Custom"
  bg="#111827" fg="#f9fafb" border="#60a5fa"
  border-width="2px" radius="0.35rem" size="md"
  padding="0.22em 0.75em" weight="750"
  shadow="0 2px 8px rgb(0 0 0 / .16)" >}}
```

**Output:** {{< badge "Beta" >}} {{< badge "Estável" type="success" icon="✓" >}} {{< badge "Experimental" type="warning" >}} {{< badge "v0.6.0" type="accent" appearance="solid" >}} {{< badge "Custom" bg="#111827" fg="#f9fafb" border="#60a5fa" border-width="2px" radius="0.35rem" size="md" padding="0.22em 0.75em" weight="750" shadow="0 2px 8px rgb(0 0 0 / .16)" >}}

A forma AST-native continua usando uma classe interna prefixada, para não colidir com `.badge` do Bootstrap:

```markdown
[AST-native]{.semantic-badge key="stable" appearance="outline" size="md"}
```

**Output:** [AST-native]{.semantic-badge key="stable" appearance="outline" size="md"}

## Steps — numerado

```markdown
:::steps
## Instale a extensão

Use `quarto add lsbjordao/quarto-semantic-components`.

## Ative o filtro

Inclua `semantic-components` em `filters`.

## Escreva normalmente

Cada heading inicia uma etapa.
:::
```

**Output:**

:::steps
## Instale a extensão

Use `quarto add lsbjordao/quarto-semantic-components`.

## Ative o filtro

Inclua `semantic-components` em `filters`.

## Escreva normalmente

Cada heading inicia uma etapa.
:::

## Steps — bolinhas

### Herdando defaults do `_quarto.yml`

```markdown
::: {.steps type="dots"}
## Extrair

Baixe os dados.

## Validar

Valide os registros.

## Publicar

Publique o resultado.
:::
```

**Output:**

::: {.steps type="dots"}
## Extrair

Baixe os dados.

## Validar

Valide os registros.

## Publicar

Publique o resultado.
:::

### Override no bloco e por etapa

```markdown
::: {.steps type="dots"
  dot-color="#8c959f"
  dot-size="0.72rem"
  line-color="#d0d7de"
  line-width="1.5px"}

## Extrair {dot-color="#2563eb" dot-size="0.62rem"}

Baixe os registros novos.

## Deduplicar {dot-color="#f59e0b" dot-size="1.1rem" line-color="#f59e0b" line-width="3px"}

Reconcilie coletas e identificações.

## Publicar {dot-color="#2da44e" dot-size="0.85rem" line-width="2px"}

Atualize a base curada.
:::
```

**Output:**

::: {.steps type="dots" dot-color="#8c959f" dot-size="0.72rem" line-color="#d0d7de" line-width="1.5px"}
## Extrair {dot-color="#2563eb" dot-size="0.62rem"}

Baixe os registros novos.

## Deduplicar {dot-color="#f59e0b" dot-size="1.1rem" line-color="#f59e0b" line-width="3px"}

Reconcilie coletas e identificações.

## Publicar {dot-color="#2da44e" dot-size="0.85rem" line-width="2px"}

Atualize a base curada.
:::

## Lista ordenada circulada

```markdown
:::circle-list
1. Primeiro item
2. Segundo item, com **Markdown** normal
3. Terceiro item
:::
```

**Output:**

:::circle-list
1. Primeiro item
2. Segundo item, com **Markdown** normal
3. Terceiro item
:::

## Git tree

A sintaxe continua sendo uma lista aninhada, mas o HTML agora usa um grafo SVG por lanes. Merges de vários níveis são desenhados sequencialmente, em vez de criar ganchos de pseudo-elementos CSS.

```markdown
:::git-tree
- `main` initial commit
- `main` cria a arquitetura base
  - `feature/icons` abre o branch de ícones
  - `feature/icons` adiciona Devicon
    - `docs/icons` documenta os providers
    - `docs/icons` adiciona exemplos
  - `feature/icons` adiciona links em arquivos
- `main` merge feature/icons
- `main` inicia componentes inline
  - `feature/badges` cria badge
  - `feature/badges` adiciona customização visual
    - `test/badges` cobre variantes
    - `test/badges` cobre links
- `main` merge feature/badges
- `main` release v0.6.0
:::
```

**Output:**

:::git-tree
- `main` initial commit
- `main` cria a arquitetura base
  - `feature/icons` abre o branch de ícones
  - `feature/icons` adiciona Devicon
    - `docs/icons` documenta os providers
    - `docs/icons` adiciona exemplos
  - `feature/icons` adiciona links em arquivos
- `main` merge feature/icons
- `main` inicia componentes inline
  - `feature/badges` cria badge
  - `feature/badges` adiciona customização visual
    - `test/badges` cobre variantes
    - `test/badges` cobre links
- `main` merge feature/badges
- `main` release v0.6.0
:::

## File tree — aninhamento profundo e ícones

O `_quarto.yml` da galeria define **Devicon como provider padrão**. Para `.qmd`, `_quarto.yml` e `quarto.yml`, a extensão usa o ícone oficial hospedado pelo próprio Quarto (`https://quarto.org/quarto.png`) porque Devicon não fornece um glifo do Quarto.

```markdown
:::file-tree
- +project
  - +src
    - +pipelines
      - +python
        - pipeline.py ingestão
        - validate.py validação
      - +r
        - analysis.R análise estatística
        - report.Rmd relatório
      - +web
        - +src
          - +components
            - chart.ts componente de gráfico
            - table.ts componente de tabela
          - app.ts aplicação
          - chart.js gráficos
          - styles.css estilos
          - index.html página
    - +database
      - +migrations
        - schema.sql esquema
        - indexes.sql índices
  - +docs
    - report.qmd relatório Quarto
  - +data
    - +raw
      - occurrences.csv dados brutos
    - +processed
      - +gold
        - occurrences.parquet base curada
  - README.md documentação
  - _quarto.yml configuração Quarto
:::
```

**Output:**

:::file-tree
- +project
  - +src
    - +pipelines
      - +python
        - pipeline.py ingestão
        - validate.py validação
      - +r
        - analysis.R análise estatística
        - report.Rmd relatório
      - +web
        - +src
          - +components
            - chart.ts componente de gráfico
            - table.ts componente de tabela
          - app.ts aplicação
          - chart.js gráficos
          - styles.css estilos
          - index.html página
    - +database
      - +migrations
        - schema.sql esquema
        - indexes.sql índices
  - +docs
    - report.qmd relatório Quarto
  - +data
    - +raw
      - occurrences.csv dados brutos
    - +processed
      - +gold
        - occurrences.parquet base curada
  - README.md documentação
  - _quarto.yml configuração Quarto
:::

## File tree — arquivos como links

```markdown
:::file-tree
- +_extensions
  - +semantic-components
    - [`steps.lua`](https://github.com/lsbjordao/quarto-semantic-components/blob/main/_extensions/semantic-components/steps.lua) steps
    - [`file-tree.lua`](https://github.com/lsbjordao/quarto-semantic-components/blob/main/_extensions/semantic-components/file-tree.lua) file tree
    - [`git-tree.lua`](https://github.com/lsbjordao/quarto-semantic-components/blob/main/_extensions/semantic-components/git-tree.lua) git tree
    - [`badges.lua`](https://github.com/lsbjordao/quarto-semantic-components/blob/main/_extensions/semantic-components/badges.lua) badges
- [`index.qmd`](https://github.com/lsbjordao/quarto-semantic-components/blob/main/index.qmd) galeria
:::
```

**Output:**

:::file-tree
- +_extensions
  - +semantic-components
    - [`steps.lua`](https://github.com/lsbjordao/quarto-semantic-components/blob/main/_extensions/semantic-components/steps.lua) steps
    - [`file-tree.lua`](https://github.com/lsbjordao/quarto-semantic-components/blob/main/_extensions/semantic-components/file-tree.lua) file tree
    - [`git-tree.lua`](https://github.com/lsbjordao/quarto-semantic-components/blob/main/_extensions/semantic-components/git-tree.lua) git tree
    - [`badges.lua`](https://github.com/lsbjordao/quarto-semantic-components/blob/main/_extensions/semantic-components/badges.lua) badges
- [`index.qmd`](https://github.com/lsbjordao/quarto-semantic-components/blob/main/index.qmd) galeria
:::

## File tree — providers e ícones customizados

```markdown
::: {.file-tree icons="devicon"}
- analysis.R
- pipeline.py
- report.qmd
- app.js
- types.ts
:::

::: {.file-tree icons="simple-icons"}
- analysis.R
- pipeline.py
- report.qmd
- app.js
- types.ts
:::

:::file-tree
- `analysis.R`{icon="devicon:r"} R
- `pipeline.py`{icon="devicon:python"} Python
- `species.csv`{icon="leaf"} dados de espécies
- `golden.parquet`{icon="★"} base ouro
- `deduplicate.py`{icon="🧬"} deduplicação
:::
```

**Devicon/default:**

::: {.file-tree icons="devicon"}
- analysis.R
- pipeline.py
- report.qmd
- app.js
- types.ts
:::

**Simple Icons:**

::: {.file-tree icons="simple-icons"}
- analysis.R
- pipeline.py
- report.qmd
- app.js
- types.ts
:::

**Customizados:**

:::file-tree
- `analysis.R`{icon="devicon:r"} R
- `pipeline.py`{icon="devicon:python"} Python
- `species.csv`{icon="leaf"} dados de espécies
- `golden.parquet`{icon="★"} base ouro
- `deduplicate.py`{icon="🧬"} deduplicação
:::

## Inspiração e formatos

As ideias de **Steps** e **File Tree** foram inspiradas em componentes de documentação do [Vocs](https://vocs.dev/), mas a implementação desta extensão é própria e orientada ao AST do Pandoc.

- **HTML**: apresentação completa, ícones, badges e Git tree em SVG;
- **PDF**: conteúdo estrutural e links são preservados;
- **DOCX**: listas, links e texto permanecem editáveis.
